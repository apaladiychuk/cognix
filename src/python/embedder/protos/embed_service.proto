syntax = "proto3";

package com.embedd;
// option go_package = "backend/core/proto;proto";

// Import the messages from the other file
// import "embed_requests.proto";

service EmbedService {
    rpc GetEmbeding (Testiamostaminchia) returns (EmbedResponse) {}
}

message Testiamostaminchia {
    string content = 1;
    string model = 2;
}

message EmbedResponse {
    repeated float vector = 1;
}
